#' @param n an integer. If \code{freq} is not NULL, \code{n} would be set as \code{sum(freq)},
#'          otherwise if \code{x} is not NULL, \code{n} would be set as \code{length(x)}.
#' @param k an integer
#' @param x an optional vector indicating item labels
#' @param freq an integer vector of item repeat frequencies
#' @param replace an logical to draw items with replacement
